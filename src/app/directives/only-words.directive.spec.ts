import { OnlyWordsDirective } from './only-words.directive';

describe('OnlyWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyWordsDirective();
    expect(directive).toBeTruthy();
  });
});
